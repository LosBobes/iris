import { srLatn } from "react-day-picker/locale";
import { srLatn as dateFnsSrLatn } from "date-fns/locale";

/** react-day-picker locale for Calendar components */
export const calendarLocale = srLatn;

/** date-fns locale for format() / formatDate() calls */
export const dateLocale = dateFnsSrLatn;
