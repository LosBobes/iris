// Package domain defines the shared data shapes used by the Iris API.
//
// These types model both request and response payloads and the internal data
// loaded from fixtures. Keep this package small and stable so the API layer and
// persistence layer can share a single vocabulary.
package domain
