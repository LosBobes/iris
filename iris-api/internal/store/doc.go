// Package store provides persistence access for the Iris API.
//
// In this starter project, the store reads JSON fixture files from disk. The
// public methods on FixtureStore are intentionally shaped so a future database-
// backed implementation can replace the fixture implementation without forcing
// changes in the HTTP layer.
package store
